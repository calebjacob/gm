# Database
DATABASE_PATH=./data/gm.sqlite
# Path to sqlite-rembed extension binary (optional; required for RAG embeddings)
# REMBED_EXTENSION_PATH=./ext/rembed0

# LLM (Game Master) — OpenAI-compatible endpoint
LLM_BASE_URL=http://localhost:1234/v1
LLM_MODEL=llama3.2
# LLM_API_KEY=

# Embeddings (sqlite-rembed, OpenAI format)
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_BASE_URL=http://localhost:1234/v1
# EMBEDDING_API_KEY=
EMBEDDING_DIMENSION=1536

# Optional: default user UUID when no auth (auto-generated if unset)
# DEFAULT_USER_ID=
